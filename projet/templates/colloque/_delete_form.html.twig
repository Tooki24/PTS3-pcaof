<form method="post" action="{{ path('colloque_delete', {'id': colloque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colloque.id) }}">
    <button class="btn">Delete</button>
</form>
