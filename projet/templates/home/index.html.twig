{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <div class="container m-4" style="height: 200px; border: 1px solid black">
        Bannier
    </div>
     <!-- TEXT ASSO -->
    <div class=" container blocAvecToutDedans">
    <div class="blocPresentationAcceuil">
    <p class="text-black text">
        PCAoF (Popular Culture Association of France) est une association qui regroupe des chercheurs et des amateurs et passionnés de culture populaire.
    </p>
    <p class="titreObj">Objectifs de l'association</p>
    <ul>
        <li>promouvoir et de développer les études et la recherche sur les cultures populaires (trans- et inter- nationales, disciplinaires, etc.)</li>
        <li>organiser des manifestations scientifiques et/ou grand public (colloques, séminaires, expositions, conférences, documentaires…)</li>
        <li>publier, de diffuser ou d’éditer des ouvrages scientifiques, essais, articles, comptes rendus, œuvres de fiction, traductions…</li>
    </ul>
    </div>
            <div class="container LastPubliRevuesAcceuil">
                <div class="blocLastPubliAcceuil">
                    <div class ="blocBleuLastPubliAcceuil">
                <p class="lastNomAcceuil">Derniere publication</p>
                    </div>
                    <p>blalbalalblablalbalblalba</p>
            {% for publication in publications %}
                <div class="row" style="border: black 1px solid; border-radius: 5%; padding: 20px; margin: 10px">
                    <div class="col">
                        <h2>{{ publication.title }}</h2>
                        <p>{{ publication.datePubli|date('d/m/Y') }}</p>
                    </div>
                    <div class="col d-flex justify-content-center align-items-center">
                        <h1>{{ publication.id }}</h1>
                    </div>
                </div>
            {% endfor %}
                </div>

                <div class="blocLastRevueAcceuil">
                    <div class ="blocBleuLastRevueAcceuil">
            <p class="lastNomAcceuil">Derniere Revues</p>
                    </div>
            <p>blalbalbalbalblaablbal</p>
            {% for revue in revues %}
                <div class="row" style="border: black 1px solid; border-radius: 5%; padding: 20px; margin: 10px">
                    <div class="col">
                        <h2>{{ revue.title }}</h2>
                        <p>{{ revue.datePubli|date('d/m/Y') }}</p>
                    </div>
                    <div class="col d-flex justify-content-center align-items-center">
                        <h1>{{ revue.id }}</h1>
                    </div>
                </div>
            {% endfor %}
                </div>
        </div>

    <div class="blocLastColoqueAcceuil ">
        <div class  ="blocBleuLastColoqueAcceuil">
        <p class="lastNomAcceuil">Colloque à venir</p>
        </div>
        <p>blalblbalblalbalblablbal</p>
        {% for colloque in colloques %}
        <div class="container mt-4">
            <h1 class="text-center">{{ colloque.name }}</h1>
            <h4>Theme ...</h4>
            <p>{{ colloque.description|u.truncate(200, ' ...', false) }}</p>
            <p>Du {{ colloque.dateD|date('d/m/Y') }} au {{ colloque.dateF|date('d/m/Y') }}</p>
            <p>Lieu : {{ colloque.lieu }}</p>
            <a class="align-top"><button type="button" class="btn btn-primary">Voir PLUS</button></a>
        </div>
        {% endfor %}
    </div>
    </div>

{% endblock %}
