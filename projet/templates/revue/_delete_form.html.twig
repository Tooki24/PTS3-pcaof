<form method="post" action="{{ path('revue_delete', {'id': revue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revue.id) }}">
    <button class="btn">Delete</button>
</form>
